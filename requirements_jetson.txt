# ── Jetson Nano (aarch64 / JetPack 4.6 — Python 3.8) ──────────────────────
# PyTorch + torchvision are installed by setup.sh (no ARM wheel on PyPI).

# follow_the_line
depthai==2.21.2.0
opencv-python-headless
numpy

# mask_generator — torch and torchvision installed by setup.sh

# racing_simulator
mlagents==0.28.0
pynput==1.7.6
pandas==1.3.5
